import { FC } from "react";
import { Text } from "react-native";

import ScreenWrapper from "../../../general/wrappers/ScreenWrapper";

const TemplateUpdateScreen: FC = () => {
  return (
    <ScreenWrapper>
      <Text>Update Template</Text>
    </ScreenWrapper>
  );
};

export default TemplateUpdateScreen;
